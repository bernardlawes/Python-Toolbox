camera_edge/
├── __init__.py               # Makes it a package
├── detector.py               # Contains the CameraEdgeDetector class
├── __main__.py               # Optional: lets you run with `python -m camera_edge`
├── utils.py                  # Put reusable helpers here
└── tools      # Put reusable helpers here
    ├── __init.py             # Put reusable helpers here
    └── file_tools.py         # Put reusable helpers here

main.py                       # Script that uses the package
